[project]
name = "setup-miner-backend"
version = "0.1.0"
dependencies = ["fastapi>=0.111", "uvicorn[standard]>=0.30", "pydantic>=2", "pandas>=2", "httpx>=0.27", "python-multipart>=0.0.9", "pytest>=8"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["app/tests"]
